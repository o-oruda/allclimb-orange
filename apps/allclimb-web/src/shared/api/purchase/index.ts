// 관련 api 정의

/**
 * 예)
 * 
 * export const postEmail = async (email: string) => {
  const response = await axiosPublicInstance.post('/mail/signup', {
    email: email,
  });

  return response;
};

 */
